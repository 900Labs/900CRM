<script lang="ts">
  /**
   * +page.svelte — Root page for 900CRM.
   * Renders the Dashboard view directly (no redirect needed for Tauri SPA).
   */
  import Dashboard from './Dashboard.svelte';
</script>

<Dashboard />
